# 작업 내역서

## 작업명

90분 프로토타입 — P2P QR 프로필 교환 (`features/p2p-qr`, Supabase 연동 기준)

## 2026-08-29 작업 내역

- `memory/기능 정의서.md` 14장 "2인분: P2P QR 프로필 교환" 범위를 기준으로 `memory/p2p-qr/작업계획서.md`를 최초 작성했다(로컬 mock 기준).
- `git pull --ff-only`로 `main` 최신 상태를 확인했다(변경 없음).
- `supabase/schema.sql`(7개 테이블: `profiles`, `parties`, `party_members`, `host_messages`, `missions`, `mission_submissions`, `rolling_messages`, RLS 정책 포함)이 이미 저장소에 존재함을 확인했다.
- `src/lib/supabase/client.js`에 브라우저 클라이언트 생성 함수는 있으나, 실제 쿼리(`.from()`, `.select()` 등)를 호출하는 코드는 저장소 어디에도 없음을 확인했다.
- 저장소에 `Quence1151`(`dlgksdyd1151@naver.com`) 명의로 push된 커밋 2건(Next.js 전환 관련)을 확인했다.
- 사용자로부터 실제 Supabase 접속 정보(`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`)를 전달받아 `.env.local`을 생성했다(`.gitignore`로 커밋 제외 확인).
- **설계를 Supabase 사용 기준으로 갱신**했다:
  - `memory/기획 개요.md` 12장에서 "서버·데이터베이스: 사용하지 않음" 전제를 제거하고 Supabase 사용으로 명시했다.
  - `memory/기술계획서.md`를 전면 갱신했다: 확정 구조에 Supabase(Postgres/Auth/Realtime) 추가, "사용하지 않는 것"에서 Supabase 항목 제거, 런타임 구조·데이터 모델(5장, `supabase/schema.sql`과 1:1 대응)·상태 관리(6장, Anonymous Auth + Realtime, `localStorage`는 보조 캐시로 격하)·인터페이스 계약(9장, adapter가 `Promise` 반환)·폴백(13장)·검증 체크리스트(14장)를 모두 Supabase 기준으로 다시 썼다.
  - 사진 미션 제출 저장 방식(`mission_submissions.content` 1000자 제약과 압축 이미지 Data URL 간 불일치)을 새 미결정 항목으로 5장에 명시했다.
  - `memory/p2p-qr/작업계획서.md`를 mock adapter 기준에서 실제 Supabase 쿼리(`profiles`, `party_members`) 기준으로 다시 작성했다. `profiles` RLS가 파티 경계를 강제하지 않는다는 점을 확인해, 같은 파티 소속 검증은 애플리케이션 코드에서 처리하도록 계획에 명시했다.

## 2026-08-29 추가 작업 내역

- `memory/기획 개요.md`, `memory/기술계획서.md`에 반영했던 Supabase 관련 수정이 원래 내용으로 되돌아간 것을 확인했다. 사용자 피드백에 따라 이후로는 공용 문서(`기획 개요.md`, `기능 정의서.md`, `기술계획서.md`)를 수정하지 않고 이 폴더(`memory/p2p-qr/`)에만 작업 내용을 기록하기로 했다.
- 2인분과 관련된 미결정 사항(`기획 개요.md` 11장의 "QR 스캔 후 프로필을 저장할지, 파티 중에만 임시 공개할지")을 확인하고, **저장하지 않고 임시 공개**하는 쪽으로 결정했다. 근거: 스캔 이력 테이블이 스키마에 없고, PROF-03 완료 기준이 이력 없이 반복 스캔만 요구함.
- 이 결정은 사용자 요청에 따라 예외적으로 `memory/기획 개요.md` 11장에도 취소선과 결정 내용으로 함께 표시했다.

## 결과

- 갱신된 최상위 기준: `memory/기획 개요.md` 12장
- 갱신된 기술 설계: `memory/기술계획서.md` (Supabase 기준 전면 개정)
- 갱신된 2인분 작업계획: `memory/p2p-qr/작업계획서.md`

## SOT 준수 점검

- [x] 핵심 정책(백엔드 사용 여부) 변경을 구현 전에 `memory/기획 개요.md`에 먼저 반영했다.
- [x] `memory/기능 정의서.md`의 기능 ID·상태값은 `supabase/schema.sql`의 `check` 제약과 이미 일치함을 확인해 별도 수정하지 않았다.
- [x] 스키마 변경이 필요한 지점(사진 저장 방식)은 임의로 구현하지 않고 미결정 항목으로 문서화했다.

## 미완료 및 후속 조치

- 사진 미션 저장 방식(Storage 업로드 vs 스키마 컬럼 추가) 결정 필요 — `memory/기술계획서.md` 5장 참고.
- `REWD-01`(보상 지급·수령 상태) 관련 컬럼/테이블이 스키마에 없음 — 후속 결정 필요.
- 실제 `features/p2p-qr` 구현은 아직 시작 전 상태(계획 단계).
