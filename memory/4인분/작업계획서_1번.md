# 작업계획서

## 작업자

`root`

## 목표

BeatTheIce 저장소에 memory 기반 SOT 운영 구조를 준비하고, 향후 모든 작업이 해당 문서와 runbook에 근거하도록 한다.

## 계획

1. 저장소의 `runbook.md`, `AGENTS.md`, `memory` 존재 여부를 확인한다.
2. `memory` 최상위에 공통 기획 개요를 작성한다.
3. `memory/root`에 작업계획서와 작업 내역서를 작성한다.
4. `runbook.md`가 공유되면 전문을 읽고 팀 스킬 및 운영 규칙을 관련 문서에 반영한다.
5. 각 작업 완료 후 결과와 memory SOT 간 위배 여부를 점검하고 작업 내역서에 기록한다.

## 완료 기준

- 필수 memory 문서 3종이 존재한다.
- 향후 작업에 적용할 SOT 우선순위와 완료 후 검증 절차가 문서화되어 있다.
- runbook 부재 상태와 후속 조치가 기록되어 있다.

---

## 2026-08-29 — Next.js 전환 작업계획

### 목표

기존 Vite + React + Express/Socket.IO 기반 party-icebreaker 앱의 기능을 그대로 유지하면서, 프로젝트를 Next.js(App Router) 기반으로 전환한다.

### 계획

1. 작업 전 `memory/기획 개요.md`를 재확인하고 SOT 원칙을 준수한다.
2. 기존 소스(`src/App.jsx`, `src/main.jsx`, `src/components`, `src/views`, `src/context`, `server/index.js`, `server/store.js`)를 전부 읽고 기능/데이터 흐름을 파악한다.
3. UI는 URL 라우팅이 아닌 tab 상태 기반 SPA 구조이므로, 리스크 최소화를 위해 `next/dynamic`(`ssr:false`)으로 클라이언트 전용 렌더링을 적용해 SSR 관련 브라우저 API 이슈를 원천 차단한다.
4. Socket.IO 실시간 기능은 Next 표준 API Route로 대체할 수 없으므로, Next 요청 핸들러 + Express REST API + Socket.IO를 한 프로세스에서 구동하는 커스텀 서버(`server.js`)로 통합한다.
5. REST API 로직(`server/index.js`)은 문구/동작 변경 없이 `server/api.js`(Express Router 팩토리)로 이전한다.
6. `package.json`, `tailwind.config.js`, `.gitignore`, `README.md`를 Next 기준으로 갱신한다.
7. 빌드(`next build`) 및 프로덕션 서버 기동 후 브라우저로 실제 화면·API·Socket.IO·미션완료 리워드 플로우를 검증한다.
8. 작업 완료 후 결과가 memory SOT와 충돌하지 않는지 점검하고 작업 내역서에 기록한다.

### 완료 기준

- 기존 화면(홈/퀘스트/스캔/롤링페이퍼/MY)과 모달, 실시간 브로드캐스트, 리워드 지급 로직이 동일하게 동작한다.
- `npm run build`가 에러 없이 성공한다.
- 실제 브라우저 테스트로 REST API 호출과 Socket.IO 이벤트가 정상 동작함을 확인한다.

---

## 2026-08-29 — 90분 프로토타입 사진 미션 반영 작업계획

### 목표

네트워크와 서버 업로드 없이 현장에서 사진을 촬영하거나 선택해 미션 결과로 제출하고, HOST가 같은 브라우저 상태에서 인증 사진을 확인할 수 있도록 90분 프로토타입 SOT를 갱신한다.

### 계획

1. 작업 전 `main`에서 `git pull --ff-only`를 수행한다.
2. `memory/기획 개요.md`의 90분 범위에 로컬 사진 제출을 추가한다.
3. `memory/기능 정의서.md`의 3인분 범위와 발표 시나리오에 사진 미션을 반영한다.
4. `memory/기술계획서.md`에 제출 타입, 사진 압축, 브라우저 보존, HOST 확인, 폴백과 검증 기준을 정의한다.
5. 문서 간 충돌과 90분 구현 금지선 위반 여부를 점검한다.
6. 작업 내역을 기록하고 커밋한 뒤 `main`에 push한다.

### 완료 기준

- 사진 미션이 3인분의 소유 범위로 명확히 지정된다.
- `촬영/선택 → 압축 미리보기 → 제출 → HOST 확인`이 필수 시연 흐름으로 문서화된다.
- 외부 이미지 업로드나 영구 저장을 추가하지 않는다.

---

## 2026-08-29 — 1인분: 앱 골격·MAIN·프로필 구현 작업계획

### 담당자

`1인분` — 기능 정의서 §14 「1인분: 앱 골격·MAIN·프로필」 전담

### 목표

Next.js(App Router) 프로젝트 기반 위에 팀 전체가 공유할 **공통 모바일 레이아웃**과 **하단 내비게이션**을 먼저 완성하고, MAIN 화면과 내 프로필 카드를 구현한다.  
다른 담당자(2·3·4인분)가 진입점 없이 기능 구현을 바로 시작할 수 있도록 초기 10분 안에 네비게이션 뼈대를 공유한다.

### 배경 (기능 정의서 §14 1인분 요약)

| 항목 | 내용 |
|------|------|
| 담당 기능 | Next.js 프로젝트 초기화, 공통 모바일 레이아웃, MAIN 화면, 내 프로필 카드, HOST/GUEST 데모 전환, 공통 내비게이션 |
| 완료 기준 | 모바일 MAIN에서 역할을 전환하고 내 프로필과 각 기능 진입점을 볼 수 있다 |
| 90분 구현 금지선 | 실제 인증, DB, 완전한 파티 CRUD |

### 세부 작업 계획

#### Phase 0: 환경 준비 & 팀 공통 합의 (0~10분)

- [ ] Next.js 15 App Router 프로젝트 생성 (`npx create-next-app@latest`)
  - Tailwind CSS 포함, App Router 사용
- [ ] 추가 의존성 설치: `lucide-react`, `qrcode.react`, `canvas-confetti`
- [ ] 팀 공통 합의 사항 고정 (10분 내)
  - 파일 경로 규칙: `app/`, `components/`, `lib/`, `types/`
  - 데모 사용자 타입 및 mock 데이터 위치 (`lib/mock.ts`)
  - 공유 상태 관리 방식: React Context + `localStorage`
  - 브랜치명 규칙 및 병합 순서

#### Phase 1: 공통 레이아웃 & 내비게이션 (10~25분)

- [ ] `app/layout.tsx` — 모바일 기준 (max-w-sm 중앙 정렬) 루트 레이아웃
- [ ] `components/BottomNav.tsx` — 5탭 하단 내비게이션
  - 탭 구성: 홈(MAIN) / 퀘스트 / QR스캔 / 롤링페이퍼 / MY프로필
  - 활성 탭 스카이 블루(`#0EA5E9`) 강조
  - 중앙 QR 버튼: 스카이 블루 그라데이션 원형
- [ ] `components/Header.tsx` — `BeattheIce 🧊` 브랜드 헤더
  - 파티원 수 배지, HOST/GUEST 역할 전환 토글
- [ ] 탭 상태를 `AppContext`로 전역 관리 (`activeTab`)
- [ ] **팀 공유**: 완성 즉시 브랜치 push하여 2·3·4인분이 바로 사용하도록 공유

#### Phase 2: 데모 데이터 & 전역 상태 (25~35분)

- [ ] `lib/mock.ts` — 데모 사용자 5명 + HOST 1명 정의
  - 필드: `id`, `name`, `role`, `avatar`, `mbti`, `bio`, `tags`, `icebreakerQuestion`, `icebreakerAnswer`, `points`, `scannedGuests`, `completedMissions`
- [ ] `lib/store.ts` — React Context 기반 전역 상태
  - 포함 상태: `currentUser`, `isHostMode`, `activeTab`, `guests`, `missions`, `popups`, `rollingPapers`
  - `localStorage` 영속성: `currentUserId`, `isHostMode`
- [ ] Header 내 HOST/GUEST 데모 사용자 전환 switcher UI

#### Phase 3: MAIN 화면 구현 (35~55분)

- [ ] `components/views/MainView.tsx`
  - **호스트 메시지 팝업 배너** (`HostPopupBanner`)
    - 타입별 시각 구분: 일반 💬 / 미션 🚨 / 결과 발표 🏆
    - 닫기 버튼 및 자동 소멸 타이머
  - **내 프로필 카드**
    - 아바타 (스카이 블루 테두리)
    - QR 코드 (`qrcode.react`, 전경색 `#0EA5E9`)
    - 이름, MBTI 배지, 한 줄 소개, 취향 태그 칩
  - **상대방 영역**
    - QR 스캔 유도 CTA 버튼 (스카이 블루)
    - 스캔 완료 후 상대 프로필 미니 카드 표시 (2인분 연결 포인트)
  - **하단 프로모 배너** (파티 퀘스트 유도)

#### Phase 4: MY 프로필 화면 (55~70분)

- [ ] `components/views/MyProfileView.tsx`
  - 대형 QR 코드 카드 (스카이 블루, `#0EA5E9` 전경)
  - 아바타, 이름, MBTI, 한 줄 소개, 태그 표시
  - 프로필 인라인 편집 (이름·소개·태그·MBTI), 저장 시 `localStorage` 반영
  - HOST 모드 시 「호스트 컨트롤 열기」 버튼 노출

#### Phase 5: 통합 점검 & 팀 연결 (70~80분)

- [ ] 2인분(QR 스캔·파트너 모달), 3인분(팝업·퀘스트 뷰), 4인분(롤링페이퍼) 진입점 연결 확인
- [ ] HOST ↔ GUEST 역할 전환 시 UI 즉시 반영 확인
- [ ] 모바일 뷰포트(375px) 레이아웃 깨짐 없는지 확인
- [ ] `localStorage` 기반 상태 새로고침 후 유지 확인

#### Phase 6: 발표 준비 & 마무리 (80~90분)

- [ ] 발표 시나리오 1~3단계 직접 시연 테스트
  - GUEST A 프로필과 QR 표시 → GUEST B QR 인식 → 상대 프로필 확인 → MBTI 대화 소재 설명
- [ ] 치명적 레이아웃 오류 수정
- [ ] 팀 통합 브랜치에 최종 push

### 기술 결정 사항

| 항목 | 결정 |
|------|------|
| 프레임워크 | Next.js 15 (App Router) |
| 스타일 | Tailwind CSS — 스카이 블루 테마 (`#0EA5E9`, `#0284C7`, `#E0F2FE`) |
| 상태 관리 | React Context + `localStorage` |
| QR 생성 | `qrcode.react` |
| 아이콘 | `lucide-react` |
| 탭 간 실시간 동기화 | `BroadcastChannel` (서버 없음, 같은 브라우저 탭 간) |
| 금지 | 외부 인증, DB, 서버 이미지 업로드 |

### 2인분 연결 인터페이스 (Mock 계약)

2인분 담당자가 QR 스캔 기능을 구현할 수 있도록 아래 함수 시그니처를 `lib/store.ts`에 미리 정의한다.

```ts
// 2인분이 호출할 스캔 결과 반영 함수
scanGuest: (targetId: string) => void;
// 스캔된 파트너 상태 (null이면 미스캔)
scannedPartner: Guest | null;
```

### 완료 기준

- [ ] `npm run dev` 실행 후 모바일 MAIN 화면이 렌더링된다.
- [ ] HOST ↔ GUEST 역할 전환 시 Header와 버튼 UI가 즉시 반영된다.
- [ ] 내 프로필 카드에 QR 코드, MBTI, 한 줄 소개, 태그 칩이 모두 표시된다.
- [ ] 하단 내비게이션 5탭이 동작하고 각 탭 진입점이 존재한다.
- [ ] 새로고침 후 역할과 현재 사용자 상태가 `localStorage`에서 복원된다.
- [ ] 발표 시나리오 1~3단계를 중단 없이 시연할 수 있다.
